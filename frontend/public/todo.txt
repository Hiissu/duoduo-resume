"""  * for list/tuple           |           ** for dictionary  
~ create new project for ... django-admin startproject core  for api/v2/  
python manage.py clearsessions

ncu                       ~ npm-check-updates
ncu -u

npm outdated
npm i -f lexical@latest
"""

redirect to     https://github.com/email_verifications if not verify yet
https://github.com/settings/emails
test4git@gmail.com


~ when login / logout / 403 response -> should clear meow / ... anything like collection reducer / word-phrase-sentences / 
~ when add new course change profile.course_learning and create a language.json contains language_code and language


remove words if words dont exist in dictionary anymore
remove word_trans if word_trans are setting as default

a trans manage page | view requested - edit - deleteeee                  also for mods and users
request words - phrases - sentences page |  for premium users            also for mods and users


load meow everytime page reload

memory mode 

disable option with unknown things when learn options shows up
~ restart | setup new practice


collection boost - collection level -   perks ~ dac quyen

config databse to save all kind of languages

~ my collections & learning|favourite collections
created list and learned list  ------------- word/pharse/sentence translations created --- word/pharse/sentence learned


      
 

https://en.wikipedia.org/wiki/List_of_ISO_639-1_codes



---- > reorganize categories 

animals
arts 
media
body 
health
clothes
communication
crime
describing things
education
food 
drink
homes 
buildings
money
natural world
people: actions
people: appearance
people: personality
politics
relationships
shopping
sports and games
technology
travel
work


function removeVietnameseTones(str) {
    str = str.replace(/à|á|ạ|ả|ã|â|ầ|ấ|ậ|ẩ|ẫ|ă|ằ|ắ|ặ|ẳ|ẵ/g,"a"); 
    str = str.replace(/è|é|ẹ|ẻ|ẽ|ê|ề|ế|ệ|ể|ễ/g,"e"); 
    str = str.replace(/ì|í|ị|ỉ|ĩ/g,"i"); 
    str = str.replace(/ò|ó|ọ|ỏ|õ|ô|ồ|ố|ộ|ổ|ỗ|ơ|ờ|ớ|ợ|ở|ỡ/g,"o"); 
    str = str.replace(/ù|ú|ụ|ủ|ũ|ư|ừ|ứ|ự|ử|ữ/g,"u"); 
    str = str.replace(/ỳ|ý|ỵ|ỷ|ỹ/g,"y"); 
    str = str.replace(/đ/g,"d");
    str = str.replace(/À|Á|Ạ|Ả|Ã|Â|Ầ|Ấ|Ậ|Ẩ|Ẫ|Ă|Ằ|Ắ|Ặ|Ẳ|Ẵ/g, "A");
    str = str.replace(/È|É|Ẹ|Ẻ|Ẽ|Ê|Ề|Ế|Ệ|Ể|Ễ/g, "E");
    str = str.replace(/Ì|Í|Ị|Ỉ|Ĩ/g, "I");
    str = str.replace(/Ò|Ó|Ọ|Ỏ|Õ|Ô|Ồ|Ố|Ộ|Ổ|Ỗ|Ơ|Ờ|Ớ|Ợ|Ở|Ỡ/g, "O");
    str = str.replace(/Ù|Ú|Ụ|Ủ|Ũ|Ư|Ừ|Ứ|Ự|Ử|Ữ/g, "U");
    str = str.replace(/Ỳ|Ý|Ỵ|Ỷ|Ỹ/g, "Y");
    str = str.replace(/Đ/g, "D");
    // Some system encode vietnamese combining accent as individual utf-8 characters
    // Một vài bộ encode coi các dấu mũ, dấu chữ như một kí tự riêng biệt nên thêm hai dòng này
    str = str.replace(/\u0300|\u0301|\u0303|\u0309|\u0323/g, ""); // ̀ ́ ̃ ̉ ̣  huyền, sắc, ngã, hỏi, nặng
    str = str.replace(/\u02C6|\u0306|\u031B/g, ""); // ˆ ̆ ̛  Â, Ê, Ă, Ơ, Ư
    // Remove extra spaces
    // Bỏ các khoảng trắng liền nhau
    str = str.replace(/ + /g," ");
    str = str.trim();
    // Remove punctuations
    // Bỏ dấu câu, kí tự đặc biệt
    str = str.replace(/!|@|%|\^|\*|\(|\)|\+|\=|\<|\>|\?|\/|,|\.|\:|\;|\'|\"|\&|\#|\[|\]|~|\$|_|`|-|{|}|\||\\/g," ");
    return str;
}
